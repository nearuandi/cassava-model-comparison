defaults:
  - paths: paths
  - dataset: cassava
  - train: train
  - model: resnet18
  - exp: base
  - _self_

seed: 42

hydra:
  # 한번
  run:
    dir: runs/${exp.name}
  # 여러번
  sweep:
    dir: runs
    subdir: ${exp.name}

# ${hydra.job.override_dirname}